{% extends 'base.html.twig' %}

{% block title %}Edit Item - {{ item.customId }}{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1>Edit {{ item.customId }}</h1>
    <div class="card mt-3">
        <div class="card-body">
            {% include 'inventory_item/_form.html.twig' with {'button_label': 'Update Item'} %}
        </div>
    </div>
</div>
{% endblock %}